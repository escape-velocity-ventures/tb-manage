name: AI Code Review
on:
  pull_request:
    types: [opened, synchronize]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write
jobs:
  ai-review:
    uses: escape-velocity-ventures/workflow-templates/.github/workflows/reusable-ai-review.yml@5adb1ac33dae6fd7aef46f8dc6d84ed4fa29f56c
    with:
      fail-on-warnings: false
      require-general-pass: false
      constrain-to-prior-findings: true
      review-provider-order: anthropic,openai,xai
    secrets:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      XAI_API_KEY: ${{ secrets.XAI_API_KEY }}
      TINKERBELLE_APP_ID: ${{ secrets.TINKERBELLE_APP_ID }}
      TINKERBELLE_APP_PRIVATE_KEY: ${{ secrets.TINKERBELLE_APP_PRIVATE_KEY }}
